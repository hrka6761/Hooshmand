<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allows http:// download URLs when the host app does not override networkSecurityConfig. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
